from random import randint
counters = {chr(ch):randint(0,100) for ch in range(ord('a'),ord('z')+1)}
print(counters)
sorted_counters=sorted(counters.keys(), key=lambda x:counters[x])
print(sorted_counters)
print(counters['a'])
